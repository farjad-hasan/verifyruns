## 1. Shell components

- [x] 1.1 `components/Footer.jsx`: `<footer className="border-t border-raised mt-auto">`, `max-w-6xl` container, logo tile + wordmark linking to `/`, links Pricing · What we store · Security · Privacy · Terms as `rp-link` (`data-testid="footer-pricing|data|security|privacy|terms"` kept); `slim` variant: one centred line "Read-only status page powered by VerifyRuns · Privacy · Terms"
- [x] 1.2 `Nav.jsx`: `variant="public"` renders logo + the mono "Public status" label and no account controls; logged-out nav hides the link matching `useLocation().pathname` (`/login` → no "Log in", `/signup` → no "Get started"); verified: `/login` nav reads "Get started" only, `/signup` reads "Log in" only; the status page nav logo sits at x = 324, the same as `/`
- [x] 1.3 Every page root becomes `min-h-screen flex flex-col` with `<Footer />` last: Landing (replacing its inline footer), Pricing, DataPage, SecurityPage, LegalPage, AuthPage, ForgotPage, ResetPage, NotFound, Dashboard, NewCheck, CheckDetail; PublicStatus uses `<Nav variant="public" />` and `<Footer slim />` (its hand-rolled header and "powered by" line removed). Verified 2026-09-03 on the local stack: every route lists the same six footer links (logo + Pricing · What we store · Security · Privacy · Terms) at 1720 px and in a 386 px frame; the auth pages still centre their form. First pass overflowed on phones (dashboard 620 px, status 694 px at 386 px): a centred `mx-auto` column inside a flex-column root shrinks to its content width. Fixed with one `.rp-page` class (`min-height: 100vh; flex column; children width: 100%`) on every root; re-measured `scrollWidth === innerWidth` on `/`, `/pricing`, `/data`, `/privacy`, `/nope`, `/dashboard`, `/checks/:id`, `/checks/new`, `/status/:token`

## 2. Prose pages

- [x] 2.1 `LegalPage.jsx`: `max-w-[65ch]` on the lede, paragraphs and lists; section titles `<h2>`; the closing link line removed; `useTitle` receives the title without its trailing full stop (verified: tabs read "Privacy policy — VerifyRuns" and "Terms of service — VerifyRuns"; the privacy lede measures 688 px ≈ 65 ch at 18 px; 8 and 7 `<h2>`s)
- [x] 2.2 `DataPage.jsx`, `SecurityPage.jsx`: section titles and the vulnerability card title `<h2>`; "true as of August 2026" leaves the lede and a mono "Last updated {date}" line follows it, with the date of the doc each page restates (`docs/what-we-store.md` 2026-08-31, `docs/security.md` 2026-09-01); the closing "See also" / link lists removed, the self-hosting sentences kept; verified: "Last updated 2026-08-31" on `/data`, "Last updated 2026-09-01" on `/security`, 6 and 8 `<h2>`s, page tails are the sentence then the footer
- [x] 2.3 `index.css` `.rp-inline` (underline, 4 px offset, hover to `#D4D4D8`, 160 ms); every `underline underline-offset-4[ hover:text-…]` on the public pages and auth pages replaced with it; `DESIGN.md` records the rule under Do's; verified: zero `underline-offset` utility strings remain on any route, the privacy policy has 4 `.rp-inline` links
- [x] 2.4 `Pricing.jsx:62`: wrap the feature text in a `<span>` so the `<li>` has exactly two flex children; verified at 1720 px: the Free card's first line reads "3 Checks · 30 runs history (planned)" inline
- [x] 2.5 `NotFound.jsx`: primary action is "Go to dashboard" when `useAuth().user` is set, "VerifyRuns home" otherwise; verified logged out: one primary "VerifyRuns home". Also in this change: the landing CTAs spell "Check" ("Create your first Check", "First Check in under five minutes") to match the app pages after `check-detail-phone`

## 3. Verify locally, then push

- [x] 3.1 `cd frontend && CI=true corepack yarn build` — 2026-09-03: compiles; 125 dependency source-map notices, the same count as `main`
- [x] 3.2 Edge walk on the local stack at 1720 px and in a 386 px frame: `/`, `/pricing`, `/data`, `/security`, `/privacy`, `/terms`, `/login`, `/signup`, `/forgot`, `/reset`, a 404, `/status/:token`, `/dashboard`, `/checks/:id`, `/checks/new`; done 2026-09-03 with the throwaway local account from `check-detail-phone`: footer on all 15 routes, nav variant right on the status page (one `<header>`), no horizontal overflow at 386 px on the nine routes measured (see 1.3)
- [x] 3.3 `openspec validate --all --strict`: 17 passed; `DESIGN.md` carries three new Do's and `.impeccable/design.json` is refreshed — `context.mjs` reports no `CONTEXT_STALE`
- [ ] 3.4 Blind review of the diff; fix or record every finding here before opening the PR
- [ ] 3.5 PR from `public-page-shell` to `main`; merge is a deploy — flip the `CLAIMS.md` row to `done` and archive in the coordination commit after merge
