## 1. Frontend

- [x] 1.1 `connectorLabel(kind)` helper; header reads "<Connector> check" for all three kinds
- [x] 1.2 Destination card renders query + masked DSN for `postgres`
- [x] 1.3 Dashboard row uses the same label

## 2. Verify locally, then push

- [x] 2.1 Production build compiles
- [x] 2.2 Postgres Check detail page shows "Postgres check", `••••••••4/vr`, and the query (checked in Edge on :3100)
