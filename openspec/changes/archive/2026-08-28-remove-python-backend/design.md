# Design

Removal only — no behaviour changes in `worker/`. A tag (`python-backend-final`) marks the last commit with `backend/` so anyone needing the FastAPI reference can `git checkout python-backend-final -- backend`. Docs are rewritten rather than trimmed where they described the Python process model (`docs/self-hosting.md`).
